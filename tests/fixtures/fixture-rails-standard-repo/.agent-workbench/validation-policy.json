{
  "validation": {
    "environment": "host",
    "host_commands": "allowed",
    "commands": [
      {
        "command": "bundle",
        "args": ["exec", "rspec"],
        "reason": "Run standard suite for hosted validation."
      }
    ]
  }
}
