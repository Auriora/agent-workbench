# Declared submodule boundary

This placeholder path is declared in the parent `.gitmodules` file.
The `.gitmodules` entry is deliberate boundary-config evidence for the bounded
config/workspace route in T007, not ordinary catalog source. This fixture path
does not contain nested Git metadata.
