# Validation protocol

The repository validation protocol admits `scripts/validate` as the
project-unit validation script for the repository-script fixture. This file is
positive evidence only; the script must not be executed by the fixture tests.
